// ====================================================================
// config.h — глобальные константы проекта GameDesk
// Версия: 1.0
// Дата: 2026-06-18
// ====================================================================

#ifndef CONFIG_H
#define CONFIG_H

// --------------------------------------------------------------------
// 1. Пины кнопок (GPIO, INPUT_PULLUP, нажатие = LOW)
// --------------------------------------------------------------------
#define BTN_LEFT    26
#define BTN_RIGHT   27
#define BTN_UP      14
#define BTN_DOWN    32
#define BTN_CENTER  13

// --------------------------------------------------------------------
// 2. Таймауты для обработки кнопок (в миллисекундах)
// --------------------------------------------------------------------
#define BTN_DEBOUNCE_MS   40    // время стабилизации сигнала (антидребезг)
#define BTN_SHORT_MAX_MS 500    // максимальная длительность короткого нажатия (< 500 мс)
#define BTN_LONG_MS     1500    // минимальная длительность долгого нажатия (>= 1500 мс)

// --------------------------------------------------------------------
// 3. Питание дисплея
//    VCC дисплея подключается к пину 5V ESP32.
//    На плате дисплея установлен стабилизатор LDO U2,
//    который понижает 5V → 3.3V для чипа ST7789V.
// --------------------------------------------------------------------
// (аппаратная конфигурация, программных констант не требует)

// --------------------------------------------------------------------
// 4. Таймауты для работы с Serial и протоколом
// --------------------------------------------------------------------
#define OFFLINE_TIMEOUT  5000   // мс без пакетов → переход в режим OFFLINE

// --------------------------------------------------------------------
// 5. Настройки Serial-протокола
// --------------------------------------------------------------------
#define SERIAL_BAUD     115200  // скорость обмена с ПК
#define PACKET_BUFFER   1024    // размер буфера для приёма пакетов (байт)

// --------------------------------------------------------------------
// 6. Цветовая схема (RGB565, 16 бит)
//    Используются стандартные константы библиотеки TFT_eSPI.
//    Для нестандартных цветов заданы приближённые значения.
// --------------------------------------------------------------------

// ---- Фоновые цвета ----
#define COLOR_BG        TFT_WHITE       // белый фон (основной)
#define COLOR_BG_HIGHLIGHT TFT_LIGHTGREY // фон для выделенного элемента (светло-серый)
#define COLOR_HIGHLIGHT TFT_BLUE        // подсветка активного пункта меню (синий)

// ---- Цвета текста ----
#define COLOR_TEXT      TFT_BLACK       // основной текст (чёрный)
#define COLOR_ACCENT    TFT_BLUE        // акцентный цвет (синий)

// ---- Цвета для телеметрии ----
#define COLOR_CPU       TFT_ORANGE      // оранжевый (температура CPU)
#define COLOR_GPU       TFT_GREEN       // зелёный (температура GPU)
#define COLOR_RAM       TFT_BLUE        // синий (использование RAM)

// ---- Специальные цвета ----
#define COLOR_OFFLINE   TFT_RED         // красный (режим OFFLINE)
#define COLOR_SEPARATOR TFT_DARKGREY    // тёмно-серый (разделители, границы)

// ---- Цвета для экрана ожидания приложения ----
#define COLOR_WAITING_BG     0x18E3    // тёмно-серый (почти чёрный)
#define COLOR_WAITING_TEXT   0x8000    // тёмно-красный


// ---- Дополнительно: если нужны оттенки ----
// (можно определить свои, если стандартных недостаточно)
// Например, оранжевый в TFT_eSPI отсутствует, но можно задать явно:
// #define COLOR_CPU 0xFD20   // или 0xFBE0

// --------------------------------------------------------------------
// 7. Прочие настройки (будущие расширения)
// --------------------------------------------------------------------
// (здесь можно добавить константы для анимаций, шрифтов и т.п.)

#endif // CONFIG_H