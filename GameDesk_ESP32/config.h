// ====================================================================
// config.h — глобальные константы проекта GameDesk
// Версия: 1.0
// Дата: 2026-06-18
// ====================================================================

#ifndef CONFIG_H
#define CONFIG_H

// --------------------------------------------------------------------
// 1. Пины кнопок (GPIO, INPUT_PULLUP, нажатие = LOW)
// --------------------------------------------------------------------
#define BTN_LEFT    26
#define BTN_RIGHT   27
#define BTN_UP      14
#define BTN_DOWN    32
#define BTN_CENTER  13

// --------------------------------------------------------------------
// 2. Таймауты для обработки кнопок (в миллисекундах)
// --------------------------------------------------------------------
#define BTN_DEBOUNCE_MS   40    // время стабилизации сигнала (антидребезг)
#define BTN_SHORT_MAX_MS 500    // максимальная длительность короткого нажатия (< 500 мс)
#define BTN_LONG_MS     1500    // минимальная длительность долгого нажатия (>= 1500 мс)

// --------------------------------------------------------------------
// 3. Питание дисплея
//    VCC дисплея подключается к пину 5V ESP32.
//    На плате дисплея установлен стабилизатор LDO U2,
//    который понижает 5V → 3.3V для чипа ST7789V.
// --------------------------------------------------------------------
// (аппаратная конфигурация, программных констант не требует)

// --------------------------------------------------------------------
// 4. Таймауты для работы с Serial и протоколом
// --------------------------------------------------------------------
#define OFFLINE_TIMEOUT  5000   // мс без пакетов → переход в режим OFFLINE

// --------------------------------------------------------------------
// 5. Настройки Serial-протокола
// --------------------------------------------------------------------
#define SERIAL_BAUD     115200  // скорость обмена с ПК
#define PACKET_BUFFER   1024    // размер буфера для приёма пакетов (байт)

// --------------------------------------------------------------------
// 6. Цветовая схема (RGB565, 16 бит)
// --------------------------------------------------------------------
// Дисплей аппаратно инвертирован и использует формат BGR.
// Макрос FIX_COLOR переводит привычный (R, G, B) [0-255] в нужный формат:
// 1. Меняет местами R и B
// 2. Инвертирует результат
#define FIX_COLOR(r, g, b) ((uint16_t)(~((((b) & 0xF8) << 8) | (((g) & 0xFC) << 3) | ((r) >> 3))))

// ---- Фоновые цвета ----
#define COLOR_BG           FIX_COLOR(15, 15, 15)      // очень тёмно-серый, почти чёрный
#define COLOR_BG_HIGHLIGHT FIX_COLOR(40, 40, 40)      // фон для выделенного элемента (тёмно-серый)
#define COLOR_HIGHLIGHT    FIX_COLOR(45, 65, 95)      // подсветка активного пункта меню (приглушённый сине-серый)

// ---- Цвета текста ----
#define COLOR_TEXT         FIX_COLOR(220, 220, 220)   // основной текст (светло-серый, не режет глаза)
#define COLOR_TEXT_DIM     FIX_COLOR(120, 120, 120)   // тусклый текст (для подсказок и второстепенной инфы)
#define COLOR_ACCENT       FIX_COLOR(100, 150, 255)   // акцентный цвет (мягкий голубой)

// ---- Цвета для телеметрии ----
#define COLOR_CPU          FIX_COLOR(255, 140, 60)    // мягкий оранжевый (температура CPU)
#define COLOR_GPU          FIX_COLOR(60, 220, 120)    // мягкий зелёный (температура GPU)
#define COLOR_RAM          FIX_COLOR(80, 180, 255)    // мягкий синий/голубой (использование RAM)
#define COLOR_FPS          FIX_COLOR(40, 180, 80)     // темно-зеленый, читаемый (FPS)

// ---- Специальные цвета ----
#define COLOR_OFFLINE      FIX_COLOR(230, 80, 80)     // мягкий красный
#define COLOR_SEPARATOR    FIX_COLOR(80, 80, 80)      // серый (разделители, границы)

// ---- Цвета для экрана ожидания приложения ----
#define COLOR_WAITING_BG   FIX_COLOR(0, 0, 0)         // чисто чёрный фон
#define COLOR_WAITING_TEXT FIX_COLOR(220, 70, 70)     // тёмно-красный текст

// ---- Цвета для анимации загрузки ----
#define COLOR_LOGO_1       FIX_COLOR(40, 10, 10)
#define COLOR_LOGO_2       FIX_COLOR(90, 20, 20)
#define COLOR_LOGO_3       FIX_COLOR(150, 40, 40)
#define COLOR_LOGO_4       FIX_COLOR(200, 60, 60)
#define COLOR_LOGO_5       FIX_COLOR(255, 80, 80)

// --------------------------------------------------------------------
// 7. Прочие настройки (будущие расширения)
// --------------------------------------------------------------------
// (здесь можно добавить константы для анимаций, шрифтов и т.п.)

#endif // CONFIG_H